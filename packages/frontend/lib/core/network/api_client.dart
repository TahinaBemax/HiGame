class ApiClient {
  final String baseUrl;

  ApiClient(this.baseUrl);

  // HTTP methods will be implemented in dedicated story
}

class WebSocketClient {
  final String serverUrl;

  WebSocketClient(this.serverUrl);

  // WebSocket connection methods will be implemented in dedicated story
}
